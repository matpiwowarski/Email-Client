<Window x:Class="E_mail_Client.NewMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:E_mail_Client"
        mc:Ignorable="d"
        Title="New Message" Height="600" Width="500">
    <Grid Background="LightGray">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>

        <Label Grid.Column="1" Grid.RowSpan="4" Foreground="#FF376DCB" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold">New Message</Label>
        <ComboBox x:Name="MailboxComboBox" Grid.Row="0" Margin="10,10,10,5">
            <ComboBoxItem x:Name="Email1" IsSelected="True">
                Email1
            </ComboBoxItem>
            <ComboBoxItem x:Name="Email2">
                Email2
            </ComboBoxItem>
        </ComboBox>

        <TextBox x:Name="RecipientTextBox" Grid.Row="1" Margin="10,5,10,5" Padding="1">
            Recipient
        </TextBox>
        <TextBox x:Name="CopyRecipientTextBox" Grid.Row="2" Margin="10,5,10,5" Padding="1">
            CopyRecipient
        </TextBox>
        <TextBox x:Name="SubjectTextBox" Grid.Row="3" Margin="10,5,10,5" Padding="1">
            Subject
        </TextBox>
        <TextBox x:Name="ContentTextBox" Grid.Row="4" Grid.ColumnSpan="2" Margin="10,5,10,10" Padding="10">
            Content
        </TextBox>

        <ListBox x:Name="AttachmentListBox" Grid.Row="5" Margin="10,5,10,10">
            
        </ListBox>
        
        <StackPanel Orientation="Horizontal"  Grid.Row="5" Grid.Column="1" HorizontalAlignment="Right">
            <Button x:Name="AttachmentButton" Margin="10,5,0,10" Click="AttachmentButton_Click">
                <Image Source="Resources/attachment.png">
                    
                </Image>
            </Button>
            <Button x:Name="SendButton" HorizontalAlignment="Right" Margin="5,5,10,10" Width="130" Click="SendButton_Click">
                Send
            </Button>
        </StackPanel>
    </Grid>
</Window>
